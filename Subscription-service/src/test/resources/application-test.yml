spring:
  datasource:
    # When using Testcontainers, the URL/User/Password are usually
    # dynamic, but keeping these as placeholders is fine.
    url: jdbc:postgresql://192.168.0.156:5432/clorameet_db
    username: clorameet_sandro
    password: algo@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
        format_sql: true

  flyway:
    enabled: false

  sql:
    init:
      mode: never
