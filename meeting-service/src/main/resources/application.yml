spring:
  profiles:
    active: local

---

spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://192.168.0.156:5432/clorameet_db
    username: clorameet_sandro
    password: algo@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # use 'validate' or 'none' in prod
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      password: ${REDIS_PASSWORD}

  mail:
    host: smtp.gmail.com
    port: 587
    username: mantu.amith@example.com
    password: 55Archuansh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8085

jwt:
  secret: Y0h4TkFxWkd6Um9TTktOVlFobU9naFN3SzY5cWhVY08=

meeting:
  expiration:
    minutes: 1440


logging:
  level:
    root: INFO
    com.algomeet.meetservice.controller: DEBUG
    com.algomeet.meetservice.service: DEBUG

algomeet:
  jwt:
    app-id: algomeet               # -> aud
    issuer: algomeet               # -> iss
    sub: meet.algomeet.com         # -> sub (your Algomeet)
    secret-base64: "gH0qKjp8oTzL8ZtW6M2w9Pj3r3y4ZK2m2b8JgQH0hQk="
    ttl-seconds: 300               # 5 minutes is typical
    features:
      file-upload: true
  idgen:
    timezone: UTC          # default; can set to Asia/Kolkata, etc.
    key-prefix: "meet:id:seq:"
    ttl-days: 10
  links:
    base: "https://meet.algoframe.in"
  cleanupScheduler:
    enabled: true

feign:
  client:
    control-service:
      url: http://localhost:8090
    user-service:
      url: http://localhost:8082

